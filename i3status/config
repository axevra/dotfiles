
general {
        colors = true
        interval = 5
}

order += "memory"
order += "cpu_usage"
order += "tztime local"


memory {
    format = " %used"
}

tztime local {
    format = "%Y-%m-%d %I:%M:%S %p"
}

cpu_usage {
    format = " %usage"
}

